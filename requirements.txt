Flask==2.3.3
requests==2.31.0
Pillow==10.0.0
googlemaps==4.10.0
python-dotenv==1.0.0
rasterio==1.3.8
scikit-image==0.21.0

# numpy 1.24 required a source build on py3.12 which needs distutils (no
# longer in stdlib).  Use a newer release where wheels exist for 3.12.
numpy>=1.25.1

# opencv has wheels for current Python versions; pin explicitly so pip can
# install without compiling.
opencv-python>=4.8.0.76

# ensure basic build tooling is available if wheel building is ever required
wheel

